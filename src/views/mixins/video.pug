mixin video(video)
 div
      h4
        a(href=`/videos/${video.id}`)=video.title
      p=video.description 
      ul
        each hashtag of video.hashtags
          li=hashtag
      small=video.createdAt
      hr